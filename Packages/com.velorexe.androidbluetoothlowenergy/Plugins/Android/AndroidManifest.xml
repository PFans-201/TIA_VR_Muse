<?xml version="1.0" encoding="utf-8"?>
<!--
  Library (merge) manifest for the embedded Velorexe BLE package.

  This deliberately declares ONLY permissions + the BLE feature so Android's manifest
  merger folds them into the app without replacing the project's main manifest (which
  carries the XR / OpenXR launcher activity). The upstream package shipped a full
  launcher <activity> here; it was removed to avoid a duplicate-launcher merge clash
  with Unity's XR-generated main manifest.

  Covers the full Muse-on-Quest permission set across Android versions:
    - pre-Android-12 (API <= 30): BLUETOOTH / BLUETOOTH_ADMIN + ACCESS_FINE_LOCATION
      (older Quest OS still gates BLE scanning behind fine location)
    - Android 12+ (API >= 31, e.g. Quest 3): BLUETOOTH_SCAN / BLUETOOTH_CONNECT
  VelorexeBleTransport requests the runtime (dangerous) ones at startup.
-->
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <!-- Pre-Android-12 -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30" />

    <!-- Android 12+ (Quest 3 and newer) -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
                     android:usesPermissionFlags="neverForLocation" tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" tools:targetApi="s" />
</manifest>
