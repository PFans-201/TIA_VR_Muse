# Python dependencies for the standalone EEG / Muse S Athena tooling.
# Install into a virtualenv:
#   python -m venv .venv
#   .venv/bin/pip install -r requirements.txt
#
# See docs/eeg-debug-guide.md for usage.

# Linux note: BrainFlow 5.22.2 cannot stream the Muse S Athena (it connects but
# fails to subscribe to the data characteristics). Use the SimpleBLE-based bridge
# instead: Tools/muse_bridge.py  (see docs/muse-unity-bridge.md).
brainflow==5.22.2
numpy==2.4.6
simplepyble==0.14.0

# Session plotting (Tools/muse_plot.py): the three 0..1 indices over time with the
# game-state transitions marked. matplotlib also covers the live re-draw mode.
matplotlib==3.11.0

# WiFi bridge (Quest 2 over hotspot) — used by Tools/muse_lsl_bridge.py
# muselsl connects to the Muse via Bluetooth on the Mac and exposes an LSL stream.
# pylsl reads that stream so the bridge can forward EEG data to the Quest over UDP.
muselsl
pylsl
